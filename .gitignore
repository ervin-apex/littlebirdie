/node_modules
/.next
/.next-uat
/tmp/
/out
/build
/.vercel
/.billing-uat-fixtures
*.log
npm-install.log
*.tsbuildinfo
.DS_Store
.env*.local
next-env.d.ts

# Local agent and design-tool state (not part of the product)
/.agents
/.codex
/.hallmark
/.impeccable

# Internal project material — kept out of the public repo
# (client meeting transcripts, requirements/architecture notes, local tooling)
/docs
/.claude

# Local build output for the throwaway dist dirs used while capturing footage
# and QA screenshots (next build -> NEXT_DIST_DIR=.next-something).
/.next-*

# Working assets and QA output from landing page and video production. These are
# generated locally and run to well over a gigabyte, so the repo keeps only the
# finished assets that ship in /public.
/output
/outputs
/landing
/design
/.media
/animation-plans
